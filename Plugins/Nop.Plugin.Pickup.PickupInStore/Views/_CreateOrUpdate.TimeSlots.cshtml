@model StorePickupTimeSlotModel
<div asp-validation-summary="All"></div>


@if (ViewBag.RefreshPage == true)
{
    <script>
        try { window.opener.document.forms['@(Context.Request.Query["formId"])'].@(Context.Request.Query["btnId"]).click(); }
        catch (e){}
        window.close();
    </script>
}
<div class="cards-group">
    <div class="card card-default card-popup">
        <div class="card-body">
            <input asp-for="StorePickupPointId" type="hidden" />
            <div class="form-group row">
                <div class="col-sm-3">
                    <nop-label asp-for="TimeSlot" />
                </div>
                <div class="col-sm-9">
                    <nop-editor asp-for="TimeSlot" />
                    <span asp-validation-for="TimeSlot"></span>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-sm-3">
                    <nop-label asp-for="LimitOrderPerTimeSlot" />
                </div>
                <div class="col-sm-9">
                    <nop-editor asp-for="LimitOrderPerTimeSlot" />
                    <span asp-validation-for="LimitOrderPerTimeSlot"></span>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-sm-3">
                    <nop-label asp-for="Active" />
                </div>
                <div class="col-sm-9">
                    <nop-editor asp-for="Active" />
                    <span asp-validation-for="Active"></span>
                </div>
            </div>
        </div>
    </div>
</div>
@*<form asp-controller="PickupInStore" asp-action="ConfigureTimeSlot" method="post" id="pickup-timeslot-form">
    <div class="cards-group">
        <div class="card card-default">
            <div class="card-body">
                @await Html.PartialAsync("Table", new DataTablesModel
                {
                Name = "pickup-timeslot-grid",
                UrlRead = new DataUrl("ListPickUpTimeSlot", "PickupInStore", null),
                UrlDelete = new DataUrl("Delete", "PickupInStore", null),
                Length = Model.StorePickupTimeSlotSearchModel.PageSize,
                LengthMenu = Model.StorePickupTimeSlotSearchModel.AvailablePageSizes,
                ColumnCollection = new List<ColumnProperty>
                {
                new ColumnProperty(nameof(StorePickupTimeSlotModel.TimeSlot))
                {
                Title = T("Plugins.Pickup.PickupInStore.Fields.TimeSlot").Text,
                Width = "200"
                },
                new ColumnProperty(nameof(StorePickupTimeSlotModel.LimitOrderPerTimeSlot))
                {
                Title = T("Plugins.Pickup.PickupInStore.Fields.LimitOrderPerTimeSlot").Text,
                Width = "200"
                },
                new ColumnProperty(nameof(StorePickupTimeSlotModel.Active))
                {
                Title = T("Plugins.Pickup.PickupInStore.Fields.Active").Text,
                Width = "100"
                }
                //,
                //new ColumnProperty(nameof(StorePickupTimeSlotModel.Id))
                //{
                //Title = T("Admin.Common.Edit").Text,
                //ClassName = NopColumnClassDefaults.Button,
                //Width = "100",
                //Render = new RenderCustom("renderColumnEdit")
                //},
                //new ColumnProperty(nameof(StorePickupTimeSlotModel.Id))
                //{
                //Title = T("Admin.Common.Delete").Text,
                //ClassName = NopColumnClassDefaults.Button,
                //Render = new RenderButtonRemove(T("Admin.Common.Delete").Text),
                //Width = "100"
                //}
                }
                })

            </div>

            <script>
                function renderColumnEdit(data, type, row, meta) {
                    return '<button onclick=\"javascript:OpenWindow(\'@Url.Content("~/Admin/PickupInStore/Edit/")' + data + '?btnId=btnRefresh&formId=pickup-timeslot-form\', 800, 800, true); return false;\" class="btn btn-default"><i class="fas fa-pencil-alt"></i>@T("Admin.Common.Edit").Text</button>';
                }
            </script>

            <div class="card-footer">
                <button type="submit" id="btnAddNewOption" name="save" class="btn btn-primary" onclick="javascript:OpenWindow('@(Url.Action("CreateTimeSlot", "PickupInStore",  new { btnId = "btnRefresh", formId = "pickup-timeslot-form" }))', 800, 800, true); return false;">@T("Plugins.Pickup.PickupInStore.AddNewTimeSlot")</button>

                <button id="btnRefresh" type="submit" name="save" class="btn btn-default" style="display: none"></button>
            </div>
            <script>
                $(document).ready(function () {
                    $('#btnRefresh').click(function () {
                        //refresh grid
                        updateTable('#pickup-timeslot-grid');

                        //return false to don't reload a page
                        return false;
                    });
                });
            </script>
        </div>
    </div>
</form>*@